function obtenerCaracter(cadena, posicion) {
    return cadena.charAt(posicion);
  }
  
  // Ejemplo de uso:
  const resultado = obtenerCaracter("JavaScript", 0);
  console.log(resultado); // Salida: "J"
  