function obtenerSubcadena(cadena, inicio, fin) {
    return cadena.substring(inicio, fin);
  }
  
  // Ejemplo de uso:
  const resultado = obtenerSubcadena("JavaScript", 4, 10);
  console.log(resultado); // Salida: "Script"
  