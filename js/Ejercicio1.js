
function longitudCadena(cadena) {
    return cadena.length; // Devuelve la longitud de la cadena
}

let texto = "Hola";
let cantidad = longitudCadena(texto);
console.log(`La longitud de la cadena "${texto}" es: ${cantidad}`);
